# Aurora Study

A six-pass demonstration of the painting contract: ground, atmospheric washes, ridges, aurora gesture, reflections and finishing light. Every compositional mark is visible in `painting.svg`, ordered with `data-stage`, and consumed by `render.py`. The renderer adds only deterministic surface grain.
